<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TodoList" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gun_metal</item>
        <item name="colorPrimaryVariant">@color/beau_blue</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lavender_web</item>
        <item name="colorSecondaryVariant">@color/lavender_web</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>